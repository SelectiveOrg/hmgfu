# Checkpoint solicitado antes da retoma GPT

Criado em 2026-09-16 a partir de `75f5618` (`phase90-diagnostic-cycle`).

- Branch de preservação solicitado: `stable-9-15-26`.
- Branch de trabalho solicitado: `stable-gpt-9-16-26`.
- Os nomes normalizam os espaços e as datas do pedido. Não há alteração retroativa das datas Git.
- `stable` identifica o checkpoint solicitado, não uma declaração de prontidão ou certificação de segurança.

## Conteúdo preservado

Todo o histórico Git existente e os quatro ficheiros pendentes revistos: `ROADMAP.md`, `docs/REFINAMENTO_AUTONOMO_SEM_LIMPEZA.md`, `.claude/launch.json`, `scripts/serve_tailscale.py`. Os dois últimos pertencem ao trabalho anterior: incluídos sem alterações no snapshot solicitado, não executados nem apresentados como implementação GPT.

Nenhuma base, credencial ou resultado privado foi acrescentado ao Git. `outputs/`, `scratch/`, `reports/`, bases de dados e ficheiros ignorados permanecem intactos no disco; este checkpoint remoto é de código/configuração/documentação, não um backup externo integral desses dados.

## Estado de partida

Wave 25 terminou com artefactos completos. Porta 1 registada como 13/13 x3; v6-DEV 7/7 x3, v5-DEV 4/4 x3, v4-DEV 8/8 x3. v3-DEV tem três artefactos; a primeira repetição foi registada como 7/8, com S4 a bloquear o selo do v7.

O diagnóstico S4 deve ser revisto antes de alterar produto: a resposta nega conhecer a empresa e pede esclarecimento; uma observação genérica contendo o projeto pode ter sido tomada pelo juiz como atribuição do nome à empresa. Trata-se de hipótese, não de falso positivo já provado.

v7 permanece rascunho não selado. Nada promovido, publicado como release, reiniciado ou limpo nesta operação.
